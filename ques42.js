// 42.  Calculate the area of a circle given the radius.

let radius = 5; // Example radius
let area = Math.PI * Math.pow(radius, 2);

console.log(`The area of the circle with radius ${radius} is ${area}.`);
